{{#*inline "toplink"}}<a href="#" style="float: right" class="h5 my-0 mt-2">top
    ↑</a>{{/inline}}

<h2 id="current">{{buttons.currentYear}} Season</h2>

<p>
  <em>
    The current season's buttons are displayed below. They are only released the
    Tuesday before the football game! Don't forget to get your hands on one
    before they are all gone.
  </em>
</p>
<table class="table table-sm">
  <thead>
    <tr>
      <th class="pe-3" style="text-align: right">School</th>
      <th>Button</th>
    </tr>
  </thead>
  <tbody>
    {{#each buttons.currentYearButtons}}
      <tr>
        <td class="align-middle pe-3" style="text-align: right">
          {{#with
            (lookup (lookup @root.buttons.schools schoolId) "color")
            as |color|
          }}
            {{!-- {{#if (equal? color "black")}}inherit{{else}}{{color}}{{/if}} --}}
            <a
              href="#{{schoolId}}"
              style="font-size: 1.25em; font-weight: bold; color: {{color}}"
            >
              {{../school}}
            </a>
          {{/with}}
        </td>
        <td>
          <a href="{{image}}">
            <img
              alt="{{label}}"
              title="{{label}}"
              src="{{image}}"
              width="100"
              height="100"
              style="border-radius: 50%"
            />
          </a>
        </td>
      </tr>
    {{/each}}
  </tbody>
</table>

<h2 id="ivies">Ivy League: Past Seasons {{> toplink}}</h2>
{{#each buttons.bySchool.ivy}}
  {{> button-table}}
{{/each}}

<h2 id="recent">Recent Seasons: Other Schools {{> toplink}}</h2>
{{#each buttons.bySchool.recent}}
  {{> button-table}}
{{/each}}

<h2 id="other">Past Seasons: Other Schools {{> toplink}}</h2>
{{#each buttons.bySchool.other}}
  {{> button-table}}
{{/each}}
{{#if (equal? color "black")}}inherit{{else}}{{color}}{{/if}}

<h2 id="special">Special Buttons {{> toplink}}</h2>
<table class="table table-sm">
  <thead>
    <tr>
      <th>Year</th>
      <th>School</th>
      <th>Button</th>
    </tr>
  </thead>
  <tbody>
    {{#each specialButtons}}
      <tr>
        <td>{{year}}</td>
        <td>{{school}}</td>
        <td>
          <a href="{{image}}">{{label}}</a>
        </td>
      </tr>
    {{/each}}
  </tbody>
</table>

<h2 id="unknown">Secret Mystery Buttons</h2>
<em>if you know what year these are please tell us &lt;3</em>
<table class="table table-sm">
  <thead>
    <tr>
      <th>Year/School</th>
      <th>Button</th>
    </tr>
  </thead>
  <tbody>
    {{#each buttons.unknown}}
      <tr>
        <td>{{about}}</td>
        <td>
          {{#if image}}
            <a href="{{image}}">{{label}}</a>
          {{else}}
            {{label}}
          {{/if}}
        </td>
      </tr>
    {{/each}}
  </tbody>
</table>
