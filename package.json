{
  "private": true,
  "scripts": {
    "start": "nodemon --watch .eleventy.js -x 'eleventy --serve'",
    "build": "eleventy",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.0-beta.8",
    "nodemon": "^2.0.15",
    "prettier": "2.5.0"
  },
  "dependencies": {
    "bootstrap": "^5.1.3",
    "js-yaml": "^4.1.0"
  }
}
